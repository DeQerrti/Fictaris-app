{
  "name": "fictaris",
  "version": "0.1.0",
  "private": true,
  "description": "Fictaris — инструмент для писателей и мастеров миров. Настольное приложение: данные лежат папкой на твоём диске.",
  "type": "module",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --publish never",
    "build:win": "electron-builder --win --publish never",
    "build:mac": "electron-builder --mac --publish never",
    "build:linux": "electron-builder --linux --publish never"
  },
  "devDependencies": {
    "electron": "^43.0.0",
    "electron-builder": "^26.0.0"
  },
  "build": {
    "appId": "ru.fictaris.app",
    "productName": "Fictaris",
    "directories": { "output": "dist" },
    "files": ["app/**/*", "core/**/*", "electron/**/*", "package.json"],
    "win": { "target": ["nsis"] },
    "mac": { "target": [{ "target": "dmg", "arch": ["arm64", "x64"] }] },
    "linux": { "target": ["AppImage"] }
  }
}
